"""Conditional-edge routing functions live here (e.g. Phase 2/4's `should_push`).
Empty in Phase 1 — the two-node graph is purely linear — kept as a file per
spec §95's prescribed layout so later phases have an obvious home."""
